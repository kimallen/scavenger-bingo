<!DOCTYPE html>
<html>
<head>
  <title>ScavengerBingo</title>
  
  <%= csrf_meta_tags %>
</head>
<body>
	<p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>
<% if current_user %>
	<%= link_to "Sign out", destroy_user_session_path %>
<% else %>
	<% link_to("Sign in", new_user_session_path) %>
<% end %>
<%= yield %>

</body>
</html>
