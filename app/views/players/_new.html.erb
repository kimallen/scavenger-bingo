<%= form_for :player, url: players_path, html: {id: "add_player"} do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>
	<%= f.label :phone %>
	<%= f.telephone_field :phone %>
	<%= f.submit "Submit" %>
<% end %>